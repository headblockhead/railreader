package darwin

import (
	"log/slog"

	"github.com/headblockhead/railreader/darwin/decoder"
)

func processSchedule(log *slog.Logger, schedule *decoder.Schedule) error {
	log.Debug("Processing schedule")
	return nil
}
